#python main.py --base configs/latent-diffusion/celebahq-ldm-vq-4-mask.yaml -t --gpus 0,

#CUDA_VISIBLE_DEVICES=<GPU_ID> python main.py --base configs/autoencoder/<config_spec>.yaml -t --gpus 0,

for a in *.jpg; do
  b=$(printf %05d.jpg ${a%.jpg})
  if [ $a != $b ]; then
    mv $a $b
  fi
done